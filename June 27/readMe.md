functions of numpy [np]:-

1.  np.max

# numpy -> numbers in python supporting c

function numpy arrays :-

# Take two arrays of same order and then find out the total difference in them.

=> [4,7,16] - [6,16,0] = [-2,-9,16]
=> -2 + -9 + 16 =>

### ufunc[universal function] in numpy :-

1.  unary (no. of operand = 1)
    - np.exp()
    - np.sin()
    - np.cos()

2.  binary (no. of operand = 2)

- np.sum()
- np.mean()
- np.minium()
- np.maximum()
