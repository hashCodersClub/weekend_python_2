### numpy packages methods

1. np.array :

- creates an array of iterative argument passed as an argument [list,tuple,range]

# 2. np.arange :

3. np.zeroes(r,c) :-
4. np.ones
5. np.matmul

### Array Properties :-

1. arr.ndim
2. arr.shape
3. arr.size

### Array Methods :-

1. arr.argmax():
   returns the index of largest value in array
2. arr.argmin():
   returns the index of smallest value
3. arr.min()
4. arr.max()
5. arr.mean()
6. arr.reshape() :
   to change the shape of the array
